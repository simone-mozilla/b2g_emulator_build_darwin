#!/bin/sh
if test "z$*" = zversion ||
   test "z$*" = z--version
then
	echo 'git-gui version 0.18.0'
else
	exec '/usr/local/Cellar/git/1.8.5.1/share/git-gui/lib/Git Gui.app/Contents/MacOS/Wish' "$0" "$@"
fi
